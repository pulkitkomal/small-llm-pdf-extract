## graphify

Before answering codebase questions, check if `graphify-out/graph.json` exists. If it does, use `graphify query "<question>"` to answer from the graph before reading files directly.

After making code changes, rebuild the graph:
```
graphify --update
```
